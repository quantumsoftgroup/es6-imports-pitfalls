import { describe, test, expect } from "@jest/globals";
import App from "./App";

describe("App", () => {
  test("fake test case", () => {
    console.log(App);
    expect(true).toBe(true);
  });
});
